#define BinsHisto_cxx

#include "../include/BinsHisto.h"

#include <TMath.h>

BinsHisto::BinsHisto(){

}


BinsHisto::~BinsHisto(){

}

void BinsHisto::InitializeBinHistograms(){

  /*  for( Int_t bp = 0; x < 3; bp++ ){
    
      for( Int_t h = 0; h < 3; h++ ){
	  for( Int_t x = 0; x < 6; x++ ){
	      for( Int_t t = 0; t < 6; t++ ){
		  for( Int_t qq = 0; qq < 3; qq++ ){

		    //		    h1_phi[bp][h][x][t][qq]->Fill(s);// new TH1D(Form("h1_phi_binned_bp%d_h%d_x%d_t%d_qq%d",bp,h,x,t,qq),Form("h1_phi_binned_bp%d_h%d_x%d_t%d_qq%d",bp,h,x,t,qq),10, 0.0, 360.0 );
		    
		  }
	      }
	  }
      }
  }
  */



}


